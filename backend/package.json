{
  "name": "vickytruck-backend-chofer",
  "version": "1.0.0",
  "private": true,
  "description": "API HTTP (Express) cloud: resuelve token del chofer, marca arribo/descarga, y expone /api/integracion/* para que Oracle/APEX local sincronice recorridos y consulte estado.",
  "type": "module",
  "engines": {
    "node": ">=20.12"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --env-file-if-exists=.env --test tests/unit/*.test.js tests/contract/*.test.js tests/integration/*.test.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mqtt": "^5.10.3"
  }
}
